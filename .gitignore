# Local, unpinned dev toolchain fetched for this session (portable Node, exported corp CA cert).
# Not part of the deliverable — see docs/09-build-report.md for how it was set up.
.tools/

# Node
app/node_modules/
app/dist/
app/coverage/

# Python
migration/__pycache__/
migration/**/__pycache__/
*.pyc

# OS
.DS_Store
Thumbs.db

# Claude Code runtime state (per-machine, per-session — not part of the deliverable)
.claude/scheduled_tasks.lock

# Feature 007: generated synthetic datasets. Reproducible byte-for-byte from data/synthetic/ plus
# the seed (FR-052), so the tens of megabytes do not belong in git — the manifest that identifies
# a dataset does, and so does its verification report under migration/reports/.
migration/synthetic/*/*
!migration/synthetic/*/manifest.json
