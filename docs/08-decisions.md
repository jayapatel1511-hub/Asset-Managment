# 08 — Decision log

Record every deviation from the spec here. Format: date · decision · why · who agreed.

| Date | Decision | Why | Agreed by |
|---|---|---|---|
| 2026-09-02 | Dataverse over SharePoint Lists (pending licence) | integrity, concurrency, offline, audit | Jay (proposed) |
| 2026-09-02 | Code App (React/TS) over canvas app | buildable and testable from a repo; Claude Code can own it end-to-end | Jay (proposed) |
| 2026-09-02 | Fabric out of scope for build; reporting via Power BI | Fabric is an analytics platform, not an app store | Jay (proposed) |
| 2026-09-02 | Skip Microsoft Forms phase | Forms cannot do lookups → typed IDs → duplicates | Jay (proposed) |
| 2026-09-02 | Asset ID = prefix + serial/sequence, immutable, no office/project encoded | 132 shared serials; tags must survive transfers | Jay (proposed) |
| 2026-09-02 | Location hierarchy supports N offices at any level, admin-managed in-app. No fixed office list in schema, app, flows or reminder config | Jay: "we need ability to add offices that we need, it can be n numbers". Four hard-coded offices would need a code change per new office | Jay (decided) |
| 2026-09-02 | Migration maps each source office value 1:1 to a like-named location, with no inference. SWO, Mississauga and Thunder Bay are seeded flat under Ontario and reparented by an admin afterwards | Follows from the N-offices decision: reparenting is now a screen action, so no asset needs a guessed home office and Q1/Q2 stop blocking migration | Jay (decided) |
| 2026-09-02 | `office_admins.csv` is replaced by an admin-to-location mapping derived from the location table | A four-row fixed file contradicts N offices; calibration reminders must fan out to whatever offices exist | Jay (decided) |
| 2026-09-02 | The 644 `Deployed or NOT Available` rows migrate as CheckedOut with no custodian, plus a one-week return sweep in the Ottawa pilot | Honest about the unknown; the sweep converts it to recorded fact through the normal return path rather than a data fix | Jay (decided) |
| 2026-09-02 | SLM pre-amp and element get their own Asset IDs, attached to the meter as permanent Components | They are calibrated separately and carry their own certificates; without own IDs that calibration cannot be recorded anywhere. Fleet grows to ~1,150 assets | Jay (decided) |
| 2026-09-02 | A SIM is a permanent Component of its modem, not a per-deployment Kit member | SIMs are fitted once and stay; components move with the parent automatically and never appear in a picker, keeping the deployment form short | Jay (decided) |
| 2026-09-02 | `eng_equipmentmodel`'s alternate key is manufacturer+model+**equipmenttype**, not manufacturer+model alone as `docs/01-data-model.md` states; `eng_name` is disambiguated with the type in parentheses whenever manufacturer+model repeats (e.g. "Instantel Micromate (Geophone)") | Discovered while building `migration/03_models.py`: "Instantel Micromate" is one product family sold as three physically different catalogue items in this fleet — the data-logger body (prefix DL-UM), its geophone sensor (GEO-UM) and its microphone accessory (SLM-UA) — each needing its own idprefix and equipmenttype, which a single eng_equipmentmodel row cannot hold. Same for Sigicom S50/C50 style families. Deduplicating on manufacturer+model alone silently merged 3 distinct catalogue rows into 1 and dropped their conflicting idprefix/equipmenttype. `docs/01-data-model.md` should be corrected to match before schema is created in Dataverse | ASSUMED, pending Jay's confirmation — flagged `// ASSUMPTION` wherever load-bearing |
| 2026-09-02 | Q4 (equipment model catalogue) executed under CLAUDE.md's authorization to proceed on all 8 open `specs/clarifications.md` items using each item's stated recommendation, recorded here as each becomes load-bearing | Explicit, recorded exception to the constitution's "no plan waits on an unresolved clarification" rule, granted for this session only | ASSUMED, pending Jay's confirmation |
| 2026-09-02 | Q6 (servers): 13 "Microsoft/Azure" rows excluded from the registry as configuration, not equipment; 3 "Instantel/Sigicom" named-appliance rows ("Vision", "Vision II", "INFRANet") kept as physical Server assets | `clarifications.md`'s Q6 recommendation is conditional ("keep as assets... *if* they are physical machines... if Azure denotes a hosted endpoint, exclude"); "Microsoft/Azure" reads as a cloud resource, the three named appliances read as physical station servers. All 5 of the fleet's on-paper Lifecycle=Retired rows are among the excluded Azure rows, so this choice alone determines whether any Retired asset survives migration — see `migration/reports/02_clean_report.md` | ASSUMED, pending Jay's confirmation — `// ASSUMPTION: Q6` |
| 2026-09-02 | Custodian resolution in migration uses a small hand-built name/initials allowlist derived from the Staff column's own 21 distinct values, not a live Entra/Dataverse directory lookup | No tenant access in this session (explicit constraint); FR-007 is still satisfied (resolve-or-leave-null-and-report, never store free text) — this is a migration-environment substitution, not a product decision. `migration/reports/02_clean_report.md` lists exactly which of the 87 non-blank Staff rows resolved by full name, which by initials (lower confidence, flagged), and which are left unresolved | Claude Code, pending Jay's confirmation before a real directory lookup replaces it in `04_load.py` for the actual Dataverse load |
| 2026-09-02 | Q8 (expected return on checkout): implemented **optional**, pre-filled at +14 days, editable/clearable | Follows `clarifications.md`'s own recommendation exactly ("a required field on the highest-frequency screen will be filled with whatever dismisses the keyboard fastest"). Consequence: `solution/flows/F4` (overdue returns) can only ever nudge about checkouts that kept a date — flagged `// ASSUMPTION: Q8` in `F4/definition.json` and in `CheckoutPage.tsx` | ASSUMED, pending Jay's confirmation |
| 2026-09-02 | Reminder cadence (unnumbered open item, `clarifications.md`): assumed **once per threshold crossing** (due-soon, then again on going overdue) until the calibration is recorded, not daily | The brief's own stated risk is noise ("the first thing that should be switched off if it becomes noisy"); once-per-crossing is the least noisy option that still guarantees at least one notification per state change. Flagged `// ASSUMPTION: cadence` in `solution/flows/F3/definition.json` | ASSUMED, pending Jay's confirmation |
| 2026-09-02 | The inactive-project rule (FR-027, feature 003, open) is implemented as **refuse outright** rather than warn-and-permit | Safe default, easily loosened later; implemented in `app/src/api/mock/index.ts`'s `submitCheckout`/`submitTransfer` (both refuse a non-Active project by name). Every migration-seeded project is Active today, so this has no visible effect until an admin closes one | ASSUMED, pending Jay's confirmation |
