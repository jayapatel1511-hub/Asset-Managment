# 05 — Unmatched calibration rows

2 row(s) had a usable calibration date but no matching asset by serial + model family; 87 more had no usable date at all (see 05_calibration_report.md for why). Every one of the source's 253 rows is accounted for in one of these two lists or the matched set (SC-005 / feature 002 FR-021).

## No matching asset found

| source_row | serial | model | reason |
|---|---|---|---|
| 108 | UA10456 | SLM | No asset with serial 'UA10456' under expected prefix(es) ['SLM-UA']. |
| 205 | UM21938 | Micromate | No asset with serial 'UM21938' under expected prefix(es) ['DL-UM', 'GEO-UM']. |

## Skipped — no usable date

| source_row | serial | model | reason |
|---|---|---|---|
| 39 | 108854 | D10 Micro | calibration_date is the literal 'N/A' (next_cal_date is '#VALUE!') — a spreadsheet formula error, no usable date at all |
| 40 | 13657 | S50 | calibration_date is blank and next_cal_date is the sentinel '1900-12-31' — this reads as an Excel date-arithmetic error (computing an interval from a blank start date), not a genuine future due date. Skipped per FR-020's 'implausible date' clause rather than loaded as a fabricated 2026 due date. NOTE: this contradicts specs/002's edge case, which describes these 40 rows as loadable with 'no calibration date but a next-due date' — this run's forensic check finds their actual next_cal_date value is the same 1900 sentinel in all 40 rows, not a real date. Flagged for Jay: if the source spreadsheet holds a real due date for these that this export corrupted, a further corrected re-export is needed. |
| 41 | 33100 | V12 | calibration_date is blank and next_cal_date is the sentinel '1900-12-31' — this reads as an Excel date-arithmetic error (computing an interval from a blank start date), not a genuine future due date. Skipped per FR-020's 'implausible date' clause rather than loaded as a fabricated 2026 due date. NOTE: this contradicts specs/002's edge case, which describes these 40 rows as loadable with 'no calibration date but a next-due date' — this run's forensic check finds their actual next_cal_date value is the same 1900 sentinel in all 40 rows, not a real date. Flagged for Jay: if the source spreadsheet holds a real due date for these that this export corrupted, a further corrected re-export is needed. |
| 42 | 107245 | D10 | calibration_date is the literal 'N/A' (next_cal_date is '#VALUE!') — a spreadsheet formula error, no usable date at all |
| 43 | 13656 | S50 | calibration_date is blank and next_cal_date is the sentinel '1900-12-31' — this reads as an Excel date-arithmetic error (computing an interval from a blank start date), not a genuine future due date. Skipped per FR-020's 'implausible date' clause rather than loaded as a fabricated 2026 due date. NOTE: this contradicts specs/002's edge case, which describes these 40 rows as loadable with 'no calibration date but a next-due date' — this run's forensic check finds their actual next_cal_date value is the same 1900 sentinel in all 40 rows, not a real date. Flagged for Jay: if the source spreadsheet holds a real due date for these that this export corrupted, a further corrected re-export is needed. |
| 44 | 35590 | V12 | calibration_date is blank and next_cal_date is the sentinel '1900-12-31' — this reads as an Excel date-arithmetic error (computing an interval from a blank start date), not a genuine future due date. Skipped per FR-020's 'implausible date' clause rather than loaded as a fabricated 2026 due date. NOTE: this contradicts specs/002's edge case, which describes these 40 rows as loadable with 'no calibration date but a next-due date' — this run's forensic check finds their actual next_cal_date value is the same 1900 sentinel in all 40 rows, not a real date. Flagged for Jay: if the source spreadsheet holds a real due date for these that this export corrupted, a further corrected re-export is needed. |
| 45 | 106943 | D10 | calibration_date is the literal 'N/A' (next_cal_date is '#VALUE!') — a spreadsheet formula error, no usable date at all |
| 46 | 14489 | S50 | calibration_date is blank and next_cal_date is the sentinel '1900-12-31' — this reads as an Excel date-arithmetic error (computing an interval from a blank start date), not a genuine future due date. Skipped per FR-020's 'implausible date' clause rather than loaded as a fabricated 2026 due date. NOTE: this contradicts specs/002's edge case, which describes these 40 rows as loadable with 'no calibration date but a next-due date' — this run's forensic check finds their actual next_cal_date value is the same 1900 sentinel in all 40 rows, not a real date. Flagged for Jay: if the source spreadsheet holds a real due date for these that this export corrupted, a further corrected re-export is needed. |
| 47 | 33140 | V12 | calibration_date is blank and next_cal_date is the sentinel '1900-12-31' — this reads as an Excel date-arithmetic error (computing an interval from a blank start date), not a genuine future due date. Skipped per FR-020's 'implausible date' clause rather than loaded as a fabricated 2026 due date. NOTE: this contradicts specs/002's edge case, which describes these 40 rows as loadable with 'no calibration date but a next-due date' — this run's forensic check finds their actual next_cal_date value is the same 1900 sentinel in all 40 rows, not a real date. Flagged for Jay: if the source spreadsheet holds a real due date for these that this export corrupted, a further corrected re-export is needed. |
| 48 | 107246 | D10 | calibration_date is the literal 'N/A' (next_cal_date is '#VALUE!') — a spreadsheet formula error, no usable date at all |
| 49 | 13595 | S50 | calibration_date is blank and next_cal_date is the sentinel '1900-12-31' — this reads as an Excel date-arithmetic error (computing an interval from a blank start date), not a genuine future due date. Skipped per FR-020's 'implausible date' clause rather than loaded as a fabricated 2026 due date. NOTE: this contradicts specs/002's edge case, which describes these 40 rows as loadable with 'no calibration date but a next-due date' — this run's forensic check finds their actual next_cal_date value is the same 1900 sentinel in all 40 rows, not a real date. Flagged for Jay: if the source spreadsheet holds a real due date for these that this export corrupted, a further corrected re-export is needed. |
| 50 | 33230 | V12 | calibration_date is blank and next_cal_date is the sentinel '1900-12-31' — this reads as an Excel date-arithmetic error (computing an interval from a blank start date), not a genuine future due date. Skipped per FR-020's 'implausible date' clause rather than loaded as a fabricated 2026 due date. NOTE: this contradicts specs/002's edge case, which describes these 40 rows as loadable with 'no calibration date but a next-due date' — this run's forensic check finds their actual next_cal_date value is the same 1900 sentinel in all 40 rows, not a real date. Flagged for Jay: if the source spreadsheet holds a real due date for these that this export corrupted, a further corrected re-export is needed. |
| 51 | 109420 | D10 | calibration_date is the literal 'N/A' (next_cal_date is '#VALUE!') — a spreadsheet formula error, no usable date at all |
| 52 | 13863 | S50 | calibration_date is blank and next_cal_date is the sentinel '1900-12-31' — this reads as an Excel date-arithmetic error (computing an interval from a blank start date), not a genuine future due date. Skipped per FR-020's 'implausible date' clause rather than loaded as a fabricated 2026 due date. NOTE: this contradicts specs/002's edge case, which describes these 40 rows as loadable with 'no calibration date but a next-due date' — this run's forensic check finds their actual next_cal_date value is the same 1900 sentinel in all 40 rows, not a real date. Flagged for Jay: if the source spreadsheet holds a real due date for these that this export corrupted, a further corrected re-export is needed. |
| 53 | 33630 | V12 | calibration_date is blank and next_cal_date is the sentinel '1900-12-31' — this reads as an Excel date-arithmetic error (computing an interval from a blank start date), not a genuine future due date. Skipped per FR-020's 'implausible date' clause rather than loaded as a fabricated 2026 due date. NOTE: this contradicts specs/002's edge case, which describes these 40 rows as loadable with 'no calibration date but a next-due date' — this run's forensic check finds their actual next_cal_date value is the same 1900 sentinel in all 40 rows, not a real date. Flagged for Jay: if the source spreadsheet holds a real due date for these that this export corrupted, a further corrected re-export is needed. |
| 54 | 109426 | D10 | calibration_date is the literal 'N/A' (next_cal_date is '#VALUE!') — a spreadsheet formula error, no usable date at all |
| 55 | 14567 | S50 | calibration_date is blank and next_cal_date is the sentinel '1900-12-31' — this reads as an Excel date-arithmetic error (computing an interval from a blank start date), not a genuine future due date. Skipped per FR-020's 'implausible date' clause rather than loaded as a fabricated 2026 due date. NOTE: this contradicts specs/002's edge case, which describes these 40 rows as loadable with 'no calibration date but a next-due date' — this run's forensic check finds their actual next_cal_date value is the same 1900 sentinel in all 40 rows, not a real date. Flagged for Jay: if the source spreadsheet holds a real due date for these that this export corrupted, a further corrected re-export is needed. |
| 56 | 107500 | D10 | calibration_date is the literal 'N/A' (next_cal_date is '#VALUE!') — a spreadsheet formula error, no usable date at all |
| 57 | 30240 | V12 | calibration_date is blank and next_cal_date is the sentinel '1900-12-31' — this reads as an Excel date-arithmetic error (computing an interval from a blank start date), not a genuine future due date. Skipped per FR-020's 'implausible date' clause rather than loaded as a fabricated 2026 due date. NOTE: this contradicts specs/002's edge case, which describes these 40 rows as loadable with 'no calibration date but a next-due date' — this run's forensic check finds their actual next_cal_date value is the same 1900 sentinel in all 40 rows, not a real date. Flagged for Jay: if the source spreadsheet holds a real due date for these that this export corrupted, a further corrected re-export is needed. |
| 58 | 109424 | D10 | calibration_date is the literal 'N/A' (next_cal_date is '#VALUE!') — a spreadsheet formula error, no usable date at all |
| 59 | 14586 | S50 | calibration_date is blank and next_cal_date is the sentinel '1900-12-31' — this reads as an Excel date-arithmetic error (computing an interval from a blank start date), not a genuine future due date. Skipped per FR-020's 'implausible date' clause rather than loaded as a fabricated 2026 due date. NOTE: this contradicts specs/002's edge case, which describes these 40 rows as loadable with 'no calibration date but a next-due date' — this run's forensic check finds their actual next_cal_date value is the same 1900 sentinel in all 40 rows, not a real date. Flagged for Jay: if the source spreadsheet holds a real due date for these that this export corrupted, a further corrected re-export is needed. |
| 60 | 32630 | V12 | calibration_date is blank and next_cal_date is the sentinel '1900-12-31' — this reads as an Excel date-arithmetic error (computing an interval from a blank start date), not a genuine future due date. Skipped per FR-020's 'implausible date' clause rather than loaded as a fabricated 2026 due date. NOTE: this contradicts specs/002's edge case, which describes these 40 rows as loadable with 'no calibration date but a next-due date' — this run's forensic check finds their actual next_cal_date value is the same 1900 sentinel in all 40 rows, not a real date. Flagged for Jay: if the source spreadsheet holds a real due date for these that this export corrupted, a further corrected re-export is needed. |
| 61 | 109245 | D10 | calibration_date is the literal 'N/A' (next_cal_date is '#VALUE!') — a spreadsheet formula error, no usable date at all |
| 62 | 14587 | S50 | calibration_date is blank and next_cal_date is the sentinel '1900-12-31' — this reads as an Excel date-arithmetic error (computing an interval from a blank start date), not a genuine future due date. Skipped per FR-020's 'implausible date' clause rather than loaded as a fabricated 2026 due date. NOTE: this contradicts specs/002's edge case, which describes these 40 rows as loadable with 'no calibration date but a next-due date' — this run's forensic check finds their actual next_cal_date value is the same 1900 sentinel in all 40 rows, not a real date. Flagged for Jay: if the source spreadsheet holds a real due date for these that this export corrupted, a further corrected re-export is needed. |
| 63 | 34750 | V12 | calibration_date is blank and next_cal_date is the sentinel '1900-12-31' — this reads as an Excel date-arithmetic error (computing an interval from a blank start date), not a genuine future due date. Skipped per FR-020's 'implausible date' clause rather than loaded as a fabricated 2026 due date. NOTE: this contradicts specs/002's edge case, which describes these 40 rows as loadable with 'no calibration date but a next-due date' — this run's forensic check finds their actual next_cal_date value is the same 1900 sentinel in all 40 rows, not a real date. Flagged for Jay: if the source spreadsheet holds a real due date for these that this export corrupted, a further corrected re-export is needed. |
| 64 | 109425 | D10 | calibration_date is the literal 'N/A' (next_cal_date is '#VALUE!') — a spreadsheet formula error, no usable date at all |
| 65 | 13778 | S50 | calibration_date is blank and next_cal_date is the sentinel '1900-12-31' — this reads as an Excel date-arithmetic error (computing an interval from a blank start date), not a genuine future due date. Skipped per FR-020's 'implausible date' clause rather than loaded as a fabricated 2026 due date. NOTE: this contradicts specs/002's edge case, which describes these 40 rows as loadable with 'no calibration date but a next-due date' — this run's forensic check finds their actual next_cal_date value is the same 1900 sentinel in all 40 rows, not a real date. Flagged for Jay: if the source spreadsheet holds a real due date for these that this export corrupted, a further corrected re-export is needed. |
| 66 | 107270 | D10 | calibration_date is the literal 'N/A' (next_cal_date is '#VALUE!') — a spreadsheet formula error, no usable date at all |
| 67 | 13593 | S50 | calibration_date is blank and next_cal_date is the sentinel '1900-12-31' — this reads as an Excel date-arithmetic error (computing an interval from a blank start date), not a genuine future due date. Skipped per FR-020's 'implausible date' clause rather than loaded as a fabricated 2026 due date. NOTE: this contradicts specs/002's edge case, which describes these 40 rows as loadable with 'no calibration date but a next-due date' — this run's forensic check finds their actual next_cal_date value is the same 1900 sentinel in all 40 rows, not a real date. Flagged for Jay: if the source spreadsheet holds a real due date for these that this export corrupted, a further corrected re-export is needed. |
| 68 | 32980 | V12 | calibration_date is blank and next_cal_date is the sentinel '1900-12-31' — this reads as an Excel date-arithmetic error (computing an interval from a blank start date), not a genuine future due date. Skipped per FR-020's 'implausible date' clause rather than loaded as a fabricated 2026 due date. NOTE: this contradicts specs/002's edge case, which describes these 40 rows as loadable with 'no calibration date but a next-due date' — this run's forensic check finds their actual next_cal_date value is the same 1900 sentinel in all 40 rows, not a real date. Flagged for Jay: if the source spreadsheet holds a real due date for these that this export corrupted, a further corrected re-export is needed. |
| 69 | 109418 | D10 | calibration_date is the literal 'N/A' (next_cal_date is '#VALUE!') — a spreadsheet formula error, no usable date at all |
| 70 | 14574 | S50 | calibration_date is blank and next_cal_date is the sentinel '1900-12-31' — this reads as an Excel date-arithmetic error (computing an interval from a blank start date), not a genuine future due date. Skipped per FR-020's 'implausible date' clause rather than loaded as a fabricated 2026 due date. NOTE: this contradicts specs/002's edge case, which describes these 40 rows as loadable with 'no calibration date but a next-due date' — this run's forensic check finds their actual next_cal_date value is the same 1900 sentinel in all 40 rows, not a real date. Flagged for Jay: if the source spreadsheet holds a real due date for these that this export corrupted, a further corrected re-export is needed. |
| 71 | 32620 | V12 | calibration_date is blank and next_cal_date is the sentinel '1900-12-31' — this reads as an Excel date-arithmetic error (computing an interval from a blank start date), not a genuine future due date. Skipped per FR-020's 'implausible date' clause rather than loaded as a fabricated 2026 due date. NOTE: this contradicts specs/002's edge case, which describes these 40 rows as loadable with 'no calibration date but a next-due date' — this run's forensic check finds their actual next_cal_date value is the same 1900 sentinel in all 40 rows, not a real date. Flagged for Jay: if the source spreadsheet holds a real due date for these that this export corrupted, a further corrected re-export is needed. |
| 72 | 107243 | D10 | calibration_date is the literal 'N/A' (next_cal_date is '#VALUE!') — a spreadsheet formula error, no usable date at all |
| 73 | 14485 | S50 | calibration_date is blank and next_cal_date is the sentinel '1900-12-31' — this reads as an Excel date-arithmetic error (computing an interval from a blank start date), not a genuine future due date. Skipped per FR-020's 'implausible date' clause rather than loaded as a fabricated 2026 due date. NOTE: this contradicts specs/002's edge case, which describes these 40 rows as loadable with 'no calibration date but a next-due date' — this run's forensic check finds their actual next_cal_date value is the same 1900 sentinel in all 40 rows, not a real date. Flagged for Jay: if the source spreadsheet holds a real due date for these that this export corrupted, a further corrected re-export is needed. |
| 74 | 35360 | V12 | calibration_date is blank and next_cal_date is the sentinel '1900-12-31' — this reads as an Excel date-arithmetic error (computing an interval from a blank start date), not a genuine future due date. Skipped per FR-020's 'implausible date' clause rather than loaded as a fabricated 2026 due date. NOTE: this contradicts specs/002's edge case, which describes these 40 rows as loadable with 'no calibration date but a next-due date' — this run's forensic check finds their actual next_cal_date value is the same 1900 sentinel in all 40 rows, not a real date. Flagged for Jay: if the source spreadsheet holds a real due date for these that this export corrupted, a further corrected re-export is needed. |
| 75 | 106932 | D10 | calibration_date is the literal 'N/A' (next_cal_date is '#VALUE!') — a spreadsheet formula error, no usable date at all |
| 76 | 13658 | S50 | calibration_date is blank and next_cal_date is the sentinel '1900-12-31' — this reads as an Excel date-arithmetic error (computing an interval from a blank start date), not a genuine future due date. Skipped per FR-020's 'implausible date' clause rather than loaded as a fabricated 2026 due date. NOTE: this contradicts specs/002's edge case, which describes these 40 rows as loadable with 'no calibration date but a next-due date' — this run's forensic check finds their actual next_cal_date value is the same 1900 sentinel in all 40 rows, not a real date. Flagged for Jay: if the source spreadsheet holds a real due date for these that this export corrupted, a further corrected re-export is needed. |
| 77 | 32350 | V12 | calibration_date is blank and next_cal_date is the sentinel '1900-12-31' — this reads as an Excel date-arithmetic error (computing an interval from a blank start date), not a genuine future due date. Skipped per FR-020's 'implausible date' clause rather than loaded as a fabricated 2026 due date. NOTE: this contradicts specs/002's edge case, which describes these 40 rows as loadable with 'no calibration date but a next-due date' — this run's forensic check finds their actual next_cal_date value is the same 1900 sentinel in all 40 rows, not a real date. Flagged for Jay: if the source spreadsheet holds a real due date for these that this export corrupted, a further corrected re-export is needed. |
| 78 | 106930 | D10 | calibration_date is the literal 'N/A' (next_cal_date is '#VALUE!') — a spreadsheet formula error, no usable date at all |
| 79 | 13596 | S50 | calibration_date is blank and next_cal_date is the sentinel '1900-12-31' — this reads as an Excel date-arithmetic error (computing an interval from a blank start date), not a genuine future due date. Skipped per FR-020's 'implausible date' clause rather than loaded as a fabricated 2026 due date. NOTE: this contradicts specs/002's edge case, which describes these 40 rows as loadable with 'no calibration date but a next-due date' — this run's forensic check finds their actual next_cal_date value is the same 1900 sentinel in all 40 rows, not a real date. Flagged for Jay: if the source spreadsheet holds a real due date for these that this export corrupted, a further corrected re-export is needed. |
| 80 | 35060 | V12 | calibration_date is blank and next_cal_date is the sentinel '1900-12-31' — this reads as an Excel date-arithmetic error (computing an interval from a blank start date), not a genuine future due date. Skipped per FR-020's 'implausible date' clause rather than loaded as a fabricated 2026 due date. NOTE: this contradicts specs/002's edge case, which describes these 40 rows as loadable with 'no calibration date but a next-due date' — this run's forensic check finds their actual next_cal_date value is the same 1900 sentinel in all 40 rows, not a real date. Flagged for Jay: if the source spreadsheet holds a real due date for these that this export corrupted, a further corrected re-export is needed. |
| 81 | 109252 | D10 | calibration_date is the literal 'N/A' (next_cal_date is '#VALUE!') — a spreadsheet formula error, no usable date at all |
| 82 | 14658 | S50 | calibration_date is blank and next_cal_date is the sentinel '1900-12-31' — this reads as an Excel date-arithmetic error (computing an interval from a blank start date), not a genuine future due date. Skipped per FR-020's 'implausible date' clause rather than loaded as a fabricated 2026 due date. NOTE: this contradicts specs/002's edge case, which describes these 40 rows as loadable with 'no calibration date but a next-due date' — this run's forensic check finds their actual next_cal_date value is the same 1900 sentinel in all 40 rows, not a real date. Flagged for Jay: if the source spreadsheet holds a real due date for these that this export corrupted, a further corrected re-export is needed. |
| 83 | 34690 | V12 | calibration_date is blank and next_cal_date is the sentinel '1900-12-31' — this reads as an Excel date-arithmetic error (computing an interval from a blank start date), not a genuine future due date. Skipped per FR-020's 'implausible date' clause rather than loaded as a fabricated 2026 due date. NOTE: this contradicts specs/002's edge case, which describes these 40 rows as loadable with 'no calibration date but a next-due date' — this run's forensic check finds their actual next_cal_date value is the same 1900 sentinel in all 40 rows, not a real date. Flagged for Jay: if the source spreadsheet holds a real due date for these that this export corrupted, a further corrected re-export is needed. |
| 84 | 112607 | D10 | calibration_date is the literal 'N/A' (next_cal_date is '#VALUE!') — a spreadsheet formula error, no usable date at all |
| 85 | 13599 | S50 | calibration_date is blank and next_cal_date is the sentinel '1900-12-31' — this reads as an Excel date-arithmetic error (computing an interval from a blank start date), not a genuine future due date. Skipped per FR-020's 'implausible date' clause rather than loaded as a fabricated 2026 due date. NOTE: this contradicts specs/002's edge case, which describes these 40 rows as loadable with 'no calibration date but a next-due date' — this run's forensic check finds their actual next_cal_date value is the same 1900 sentinel in all 40 rows, not a real date. Flagged for Jay: if the source spreadsheet holds a real due date for these that this export corrupted, a further corrected re-export is needed. |
| 86 | 109251 | D10 | calibration_date is the literal 'N/A' (next_cal_date is '#VALUE!') — a spreadsheet formula error, no usable date at all |
| 87 | 13648 | S50 | calibration_date is blank and next_cal_date is the sentinel '1900-12-31' — this reads as an Excel date-arithmetic error (computing an interval from a blank start date), not a genuine future due date. Skipped per FR-020's 'implausible date' clause rather than loaded as a fabricated 2026 due date. NOTE: this contradicts specs/002's edge case, which describes these 40 rows as loadable with 'no calibration date but a next-due date' — this run's forensic check finds their actual next_cal_date value is the same 1900 sentinel in all 40 rows, not a real date. Flagged for Jay: if the source spreadsheet holds a real due date for these that this export corrupted, a further corrected re-export is needed. |
| 88 | 33640 | V12 | calibration_date is blank and next_cal_date is the sentinel '1900-12-31' — this reads as an Excel date-arithmetic error (computing an interval from a blank start date), not a genuine future due date. Skipped per FR-020's 'implausible date' clause rather than loaded as a fabricated 2026 due date. NOTE: this contradicts specs/002's edge case, which describes these 40 rows as loadable with 'no calibration date but a next-due date' — this run's forensic check finds their actual next_cal_date value is the same 1900 sentinel in all 40 rows, not a real date. Flagged for Jay: if the source spreadsheet holds a real due date for these that this export corrupted, a further corrected re-export is needed. |
| 89 | 109248 | D10 | calibration_date is the literal 'N/A' (next_cal_date is '#VALUE!') — a spreadsheet formula error, no usable date at all |
| 90 | 14488 | S50 | calibration_date is blank and next_cal_date is the sentinel '1900-12-31' — this reads as an Excel date-arithmetic error (computing an interval from a blank start date), not a genuine future due date. Skipped per FR-020's 'implausible date' clause rather than loaded as a fabricated 2026 due date. NOTE: this contradicts specs/002's edge case, which describes these 40 rows as loadable with 'no calibration date but a next-due date' — this run's forensic check finds their actual next_cal_date value is the same 1900 sentinel in all 40 rows, not a real date. Flagged for Jay: if the source spreadsheet holds a real due date for these that this export corrupted, a further corrected re-export is needed. |
| 91 | 35640 | V12 | calibration_date is blank and next_cal_date is the sentinel '1900-12-31' — this reads as an Excel date-arithmetic error (computing an interval from a blank start date), not a genuine future due date. Skipped per FR-020's 'implausible date' clause rather than loaded as a fabricated 2026 due date. NOTE: this contradicts specs/002's edge case, which describes these 40 rows as loadable with 'no calibration date but a next-due date' — this run's forensic check finds their actual next_cal_date value is the same 1900 sentinel in all 40 rows, not a real date. Flagged for Jay: if the source spreadsheet holds a real due date for these that this export corrupted, a further corrected re-export is needed. |
| 92 | 106948 | D10 | calibration_date is the literal 'N/A' (next_cal_date is '#VALUE!') — a spreadsheet formula error, no usable date at all |
| 93 | 12703 | S50 | calibration_date is blank and next_cal_date is the sentinel '1900-12-31' — this reads as an Excel date-arithmetic error (computing an interval from a blank start date), not a genuine future due date. Skipped per FR-020's 'implausible date' clause rather than loaded as a fabricated 2026 due date. NOTE: this contradicts specs/002's edge case, which describes these 40 rows as loadable with 'no calibration date but a next-due date' — this run's forensic check finds their actual next_cal_date value is the same 1900 sentinel in all 40 rows, not a real date. Flagged for Jay: if the source spreadsheet holds a real due date for these that this export corrupted, a further corrected re-export is needed. |
| 94 | 32420 | V12 | calibration_date is blank and next_cal_date is the sentinel '1900-12-31' — this reads as an Excel date-arithmetic error (computing an interval from a blank start date), not a genuine future due date. Skipped per FR-020's 'implausible date' clause rather than loaded as a fabricated 2026 due date. NOTE: this contradicts specs/002's edge case, which describes these 40 rows as loadable with 'no calibration date but a next-due date' — this run's forensic check finds their actual next_cal_date value is the same 1900 sentinel in all 40 rows, not a real date. Flagged for Jay: if the source spreadsheet holds a real due date for these that this export corrupted, a further corrected re-export is needed. |
| 95 | 107411 | C22 | calibration_date is blank and next_cal_date is the sentinel '1900-12-31' — this reads as an Excel date-arithmetic error (computing an interval from a blank start date), not a genuine future due date. Skipped per FR-020's 'implausible date' clause rather than loaded as a fabricated 2026 due date. NOTE: this contradicts specs/002's edge case, which describes these 40 rows as loadable with 'no calibration date but a next-due date' — this run's forensic check finds their actual next_cal_date value is the same 1900 sentinel in all 40 rows, not a real date. Flagged for Jay: if the source spreadsheet holds a real due date for these that this export corrupted, a further corrected re-export is needed. |
| 96 | 106944 | D10 | calibration_date is the literal 'N/A' (next_cal_date is '#VALUE!') — a spreadsheet formula error, no usable date at all |
| 97 | 14614 | S50 | calibration_date is blank and next_cal_date is the sentinel '1900-12-31' — this reads as an Excel date-arithmetic error (computing an interval from a blank start date), not a genuine future due date. Skipped per FR-020's 'implausible date' clause rather than loaded as a fabricated 2026 due date. NOTE: this contradicts specs/002's edge case, which describes these 40 rows as loadable with 'no calibration date but a next-due date' — this run's forensic check finds their actual next_cal_date value is the same 1900 sentinel in all 40 rows, not a real date. Flagged for Jay: if the source spreadsheet holds a real due date for these that this export corrupted, a further corrected re-export is needed. |
| 98 | 107247 | D10 | calibration_date is the literal 'N/A' (next_cal_date is '#VALUE!') — a spreadsheet formula error, no usable date at all |
| 99 | 14603 | S50 | calibration_date is blank and next_cal_date is the sentinel '1900-12-31' — this reads as an Excel date-arithmetic error (computing an interval from a blank start date), not a genuine future due date. Skipped per FR-020's 'implausible date' clause rather than loaded as a fabricated 2026 due date. NOTE: this contradicts specs/002's edge case, which describes these 40 rows as loadable with 'no calibration date but a next-due date' — this run's forensic check finds their actual next_cal_date value is the same 1900 sentinel in all 40 rows, not a real date. Flagged for Jay: if the source spreadsheet holds a real due date for these that this export corrupted, a further corrected re-export is needed. |
| 100 | 106942 | D10 | calibration_date is the literal 'N/A' (next_cal_date is '#VALUE!') — a spreadsheet formula error, no usable date at all |
| 101 | 33290 | V12 | calibration_date is blank and next_cal_date is the sentinel '1900-12-31' — this reads as an Excel date-arithmetic error (computing an interval from a blank start date), not a genuine future due date. Skipped per FR-020's 'implausible date' clause rather than loaded as a fabricated 2026 due date. NOTE: this contradicts specs/002's edge case, which describes these 40 rows as loadable with 'no calibration date but a next-due date' — this run's forensic check finds their actual next_cal_date value is the same 1900 sentinel in all 40 rows, not a real date. Flagged for Jay: if the source spreadsheet holds a real due date for these that this export corrupted, a further corrected re-export is needed. |
| 143 | 108052 | D10 Micro | calibration_date is the literal 'N/A' (next_cal_date is '#VALUE!') — a spreadsheet formula error, no usable date at all |
| 149 | 109564 | D10 Micro | calibration_date is the literal 'N/A' (next_cal_date is '#VALUE!') — a spreadsheet formula error, no usable date at all |
| 151 | 106788 | D10 Micro | calibration_date is the literal 'N/A' (next_cal_date is '#VALUE!') — a spreadsheet formula error, no usable date at all |
| 153 | 108859 | D10 Micro | calibration_date is the literal 'N/A' (next_cal_date is '#VALUE!') — a spreadsheet formula error, no usable date at all |
| 162 | 108201 | D10 Micro | calibration_date is the literal 'N/A' (next_cal_date is '#VALUE!') — a spreadsheet formula error, no usable date at all |
| 165 | 109561 | D10 Micro | calibration_date is the literal 'N/A' (next_cal_date is '#VALUE!') — a spreadsheet formula error, no usable date at all |
| 168 | 108194 | D10 Micro | calibration_date is the literal 'N/A' (next_cal_date is '#VALUE!') — a spreadsheet formula error, no usable date at all |
| 172 | 108857 | D10 Micro | calibration_date is the literal 'N/A' (next_cal_date is '#VALUE!') — a spreadsheet formula error, no usable date at all |
| 175 | 106796 | D10 Micro | calibration_date is the literal 'N/A' (next_cal_date is '#VALUE!') — a spreadsheet formula error, no usable date at all |
| 178 | 106791 | D10 Micro | calibration_date is the literal 'N/A' (next_cal_date is '#VALUE!') — a spreadsheet formula error, no usable date at all |
| 181 | 106795 | D10 Micro | calibration_date is the literal 'N/A' (next_cal_date is '#VALUE!') — a spreadsheet formula error, no usable date at all |
| 186 | 109559 | D10 Micro | calibration_date is the literal 'N/A' (next_cal_date is '#VALUE!') — a spreadsheet formula error, no usable date at all |
| 189 | 109246 | D10 Black Case | calibration_date is the literal 'N/A' (next_cal_date is '#VALUE!') — a spreadsheet formula error, no usable date at all |
| 192 | 108856 | D10 Micro | calibration_date is the literal 'N/A' (next_cal_date is '#VALUE!') — a spreadsheet formula error, no usable date at all |
| 195 | 107860 | D10 Micro | calibration_date is the literal 'N/A' (next_cal_date is '#VALUE!') — a spreadsheet formula error, no usable date at all |
| 198 | 112604 | D10 Black Case | calibration_date is the literal 'N/A' (next_cal_date is '#VALUE!') — a spreadsheet formula error, no usable date at all |
| 201 | 108840 | D10 Micro | calibration_date is the literal 'N/A' (next_cal_date is '#VALUE!') — a spreadsheet formula error, no usable date at all |
| 206 | 106794 | D10 Micro | calibration_date is the literal 'N/A' (next_cal_date is '#VALUE!') — a spreadsheet formula error, no usable date at all |
| 208 | 106793 | D10 Micro | calibration_date is the literal 'N/A' (next_cal_date is '#VALUE!') — a spreadsheet formula error, no usable date at all |
| 229 | N/A | C50 | calibration_date is the literal 'N/A' (next_cal_date is '#VALUE!') — a spreadsheet formula error, no usable date at all |
| 237 | 106790 | D10 Micro | calibration_date is the literal 'N/A' (next_cal_date is '#VALUE!') — a spreadsheet formula error, no usable date at all |
| 242 | 108049 | D10 Micro | calibration_date is the literal 'N/A' (next_cal_date is '#VALUE!') — a spreadsheet formula error, no usable date at all |
| 244 | 107861 | D10 Micro | calibration_date is the literal 'N/A' (next_cal_date is '#VALUE!') — a spreadsheet formula error, no usable date at all |
| 247 | 106792 | D10 Micro | calibration_date is the literal 'N/A' (next_cal_date is '#VALUE!') — a spreadsheet formula error, no usable date at all |
