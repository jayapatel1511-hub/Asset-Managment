model Model
	culture: en-US
	defaultPowerBIDataSourceVersion: powerBI_V3
	sourceQueryCulture: en-US
	discourageImplicitMeasures

annotation __TEdtVersion = 1

/// Feature 006 (Fleet Reporting), WS-B. Every table below DirectQuery-connects to one Dataverse
/// table from docs/01-data-model.md (the `eng_*` prefix is stripped here the same way
/// migration/staged/*.json and app/src/api/types.ts strip it — one convention, three consumers).
/// This project has never been opened in Power BI Desktop (no tenant this session, per
/// plan.md's Constraints) — see ../../README.md for what that means and does not mean.

ref table Location
ref table EquipmentModel
ref table Project
ref table Asset
ref table Transaction
ref table TransactionLine
ref table AssetRelationship
ref table CalibrationRecord
ref table DimDate
ref table _Measures

ref culture en-US

ref expression EnvironmentUrl

